spring:
  application:
    name: "configserver"
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: "file:////Users/oby/Documents/config/eazybank"
        #search-locations: "classpath:/config"

server:
  port: 8071

